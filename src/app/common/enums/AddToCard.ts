
export enum AddToCard {
    // Не добавлять в карточку объявления
    RemoveFromCard,
    // Добавить в карточку объявления (вверх и вниз)
    AddToCardBoth,
    // Добавить в карточку объявления (только вверх)
    AddToTopOnly,
    // Добавить в карточку объявления (только вниз)
    AddToBottomOnly
}
